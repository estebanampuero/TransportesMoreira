# ─── Database ────────────────────────────────────────────────────────────────
# Internal Docker hostname when deployed in the same EasyPanel project
DB_HOST=postgresql1
DB_PORT=5432
DB_NAME=postgresql
DB_USER=postgres
DB_PASSWORD=99iwxbnlrvcpv4fvnpyf

# ─── Auth ─────────────────────────────────────────────────────────────────────
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# ─── GPS devices ──────────────────────────────────────────────────────────────
# Shared key sent by GPS hardware in X-GPS-Key header
GPS_API_KEY=gps_secret_key_change_me

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=4000

# ─── CORS — comma-separated list of allowed frontend origins ─────────────────
ALLOWED_ORIGINS=https://transportesmoreira.web.app,https://transportesmoreira.cl
